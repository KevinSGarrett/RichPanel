# Test Strategy and Matrix

Last updated: 2025-12-21

We will define:
- unit tests (routing logic, parsing)
- integration tests (Richpanel webhook → middleware)
- contract tests (payload schemas)
- LLM eval tests (golden set)
- load tests (spike + soak)
