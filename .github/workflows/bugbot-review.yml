name: "Bugbot Review Trigger [DEPRECATED]"

# DEPRECATED: This workflow is no longer used.
# Cursor Bugbot has been replaced by the in-repo PR-Agent advisory review
# (see .github/workflows/pr-agent.yml).
#
# To restore Bugbot behavior, revert this commit or re-enable the original
# workflow_dispatch trigger below. See docs/08_Engineering/PR_Checks_Rollback_Playbook.md.

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "[DEPRECATED] This workflow is disabled. Use PR-Agent instead."
        required: true
        type: string

jobs:
  deprecated:
    name: "Bugbot [DEPRECATED]"
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "::warning::Bugbot Review Trigger is DEPRECATED."
          echo "Cursor Bugbot has been replaced by the in-repo PR-Agent advisory review."
          echo "See .github/workflows/pr-agent.yml and docs/08_Engineering/PR_Checks_Rollback_Playbook.md."
          echo ""
          echo "## Bugbot — DEPRECATED" >> "$GITHUB_STEP_SUMMARY"
          echo "This workflow is **deprecated**. Use PR-Agent instead." >> "$GITHUB_STEP_SUMMARY"
