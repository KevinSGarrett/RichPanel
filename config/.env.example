# Example env file (safe to commit)
# Copy to .env locally and fill secrets there; do NOT commit .env
# Secrets stay blank here - they come from AWS Secrets Manager (`rp-mw/<env>/...`).
# Feature flags live in AWS Systems Manager Parameter Store (`/rp-mw/<env>/safe_mode`, `/rp-mw/<env>/automation_enabled`).

RICH_PANEL_ENV=dev
AWS_REGION=us-east-2
WEBHOOK_ENDPOINT_PATH=/richpanel/inbound

# Richpanel integration
RICH_PANEL_API_BASE_URL=https://api.richpanel.com
RICH_PANEL_API_KEY=
RICH_PANEL_WEBHOOK_TOKEN=

# LLM / OpenAI
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_KEY=

# Shipping / commerce integrations
SHIPSTATION_API_BASE_URL=https://ssapi.shipstation.com
SHIPSTATION_API_KEY=
SHIPSTATION_API_SECRET=

# Email provider / SMTP bridge
EMAIL_PROVIDER=
EMAIL_SMTP_HOST=
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USERNAME=
EMAIL_SMTP_PASSWORD=

# Feature flags managed via AWS SSM -> no local env values required
