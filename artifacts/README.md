# Artifacts (large / generated)

Store large outputs here (Playwright reports, large logs, big JSON dumps, etc.).

**Rule:** do not copy large artifacts into `REHYDRATION_PACK/`.  
Instead, store a short pointer/path inside `REHYDRATION_PACK/RUNS/<RUN_ID>/...`.

