# Runs (optional full archive)

Optional place for storing *full* run outputs (beyond the lightweight `REHYDRATION_PACK/RUNS/` reports).

If you keep large files here, consider adding them to `.gitignore`.

